<%  @slaves.each_with_index { |(name, nic),index| -%>
    SUBSYSTEM=="net", ACTION=="add", DRIVERS=="?*", ATTR{address}=="<%= nic['macaddress'] %>", ATTR{type}=="1", KERNEL=="eth*", NAME="eth<%= index %>"
<% } -%>
